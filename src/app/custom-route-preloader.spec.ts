import { CustomRoutePreloader } from './custom-route-preloader';

describe('CustomRoutePreloader', () => {
  it('should create an instance', () => {
    expect(new CustomRoutePreloader()).toBeTruthy();
  });
});
